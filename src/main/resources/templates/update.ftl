package ${config.basePackage}.${config.update};

import lombok.Getter;
import lombok.Setter;

import ${config.basePackage}.${config.bean}.${table.beanName};

@Getter
@Setter
public class ${table.beanName}Update extends ${table.beanName} {

}
