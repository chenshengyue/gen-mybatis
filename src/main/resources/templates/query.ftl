package ${config.basePackage}.${config.query};

import lombok.Getter;
import lombok.Setter;

import ${config.basePackage}.${config.bean}.${table.beanName};

@Getter
@Setter
public class ${table.beanName}Query extends ${table.beanName} {

}
